# 🧠 Universal Keyboard Layouts for macOS

Этот репозиторий содержит **универсальные раскладки клавиатуры для macOS**, построенные по принципу - все знаки препинания и спецсимволы находятся **на одних и тех же местах**,  независимо от выбранного языка ввода — **русский или английский**.

Это позволяет спокойно переключать язык для текста, не теряя muscle memory для символов вроде `.,?!@#$%` и т.д.

Вдохновлялся [здесь](https://tonsky.livejournal.com/318571.html)


## 🧩 Структура репозитория
- `universal-ru.keylayout` — русская раскладка
- `universal-en.keylayout` — английская раскладка

## 📦 Установка
- Копируем файлы раскладок ```bash cp *.keylayout ~/Library/Keyboard\ Layouts/```
- Смотрим текущий конфиг (опционально) ```bash defaults read ~/Library/Preferences/com.apple.HIToolbox AppleEnabledInputSources```
- Устанавливаем только кастомные раскладки
```bash
defaults write ~/Library/Preferences/com.apple.HIToolbox AppleEnabledInputSources -array \
'{"InputSourceKind" = "Keyboard Layout"; "KeyboardLayout ID" = 25251; "KeyboardLayout Name" = "universal-en"; }' \
'{"InputSourceKind" = "Keyboard Layout"; "KeyboardLayout ID" = 25252; "KeyboardLayout Name" = "universal-ru"; }' \
'{"Bundle ID" = "com.apple.CharacterPaletteIM"; InputSourceKind = "Non Keyboard Input Method"; }'
```
- Перезагружаемся
- Проверяем ```bash defaults read ~/Library/Preferences/com.apple.HIToolbox AppleEnabledInputSources```

- ID раскладок: 25251 (english), 25252 (russian) - задаются в XML-файлах раскладок
- Обязательная перезагрузка: Изменения вступят в силу только после перезагрузки
- Резервное копирование: Рекомендуется сделать бэкап перед редактированием системных файлов
